<style>
  li{
    padding: 15px 5px;
  }
  .active{
    border-bottom: 5px solid #ff171c;
  }
</style>
<nav class="navbar navbar-expand-lg navbar-light bg-success p-0">
    <div class="container-fluid">
      <!-- logo -->
      <a class="navbar-brand" href="/project-details">
        <img src="images/logos/logo.png" alt="logo image" width="50%">
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item <% if (data.pageUrl == '/') { %> active <% } %>">
            <a class="nav-link" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item <% if (data.pageUrl == '/about') { %> active <% } %>">
            <a class="nav-link" href="/about">About</a>
          </li>
          <li class="nav-item <% if (data.pageUrl == '/services') { %> active <% } %>">
            <a class="nav-link" href="/services">Services</a>
          </li>
          <li class="nav-item <% if (data.pageUrl == '/team') { %> active <% } %>">
            <a class="nav-link" href="team">Team</a>
          </li>
          <li class="nav-item <% if (data.pageUrl == '/contact') { %> active <% } %>">
            <a class="nav-link" href="contact">Contact Us</a>
          </li>
          <li class="nav-item <% if (data.pageUrl == '/project-details') { %> active <% } %>">
            <a class="nav-link" href="project-details">Project Info</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>